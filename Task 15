let num = String(prompt(`Enter a nuumber`));
let newNum = ``;
for(let i = 0; i < num.length; i++) {
if (i === 0 ) {
  newNum += num[num.length - 1]
} else if(i === num.length-1) {
  newNum += num[0]
} else{
  newNum += num[i]
}
}
console.log(newNum);
